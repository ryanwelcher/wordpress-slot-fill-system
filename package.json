{
  "name": "extending-gutenberg",
  "version": "2.0.0",
  "description": "A plugin to demonstrate extension points in Gutenberg",
  "scripts": {
	"setup": "npm install && npm run build",
    "build": "wp-scripts build --output-path=dist",
    "watch": "wp-scripts start --output-path=dist"
  },
  "author": "Ryan Welcher",
  "license": "GPL 2.0",
  "devDependencies": {
    "@wordpress/scripts": "^10.0.0",
    "html-loader": "^1.1.0",
    "marked": "^1.0.0",
    "react-markdown": "^4.3.1",
    "webpackbar": "^4.0.0"
  },
  "dependencies": {
    "react-google-charts": "^3.0.15",
    "react-syntax-highlighter": "^12.2.1",
    "uuidv4": "^6.0.8"
  }
}
